{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/retailpulse-web-application/browser",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}